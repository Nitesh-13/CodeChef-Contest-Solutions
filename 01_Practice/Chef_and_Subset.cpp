#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long t;
    cin>>t;
    while(t--)
    {
        int a,b,c,d;
        cin>>a>>b>>c>>d;
        if(a>0 && b>0 && c>0 && d>0)
        {
            cout<<"No\n";
        }
        else{
            if(a==0 || b==0 || c==0 || d == 0)
            {
                cout<<"Yes\n";
            }
            else{
                
            }
        }
    }
    return 0;
}